# Production Environment Variables - TEMPLATE
# ⚠️ Bu dosya template'tir. Gerçek değerleri .env.production dosyasına kopyalayın
# ⚠️ .env.production dosyası .gitignore'da olmalı ve asla GitHub'a commitlenmemeli!

# Port number
PORT=3000

# App URL - Kendi domain'inizi girin
APP_URL=https://yourdomain.com

# PostgreSQL Database Connection
# Kendi database connection string'inizi girin
DATABASE_URL=postgresql://username:password@host:port/database_name

# JWT Configuration
# ⚠️ PRODUCTION İÇİN MUTLAKA YENİ GÜÇLÜ SECRET OLUŞTURUN!
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-min-32-characters
JWT_ACCESS_EXPIRATION_MINUTES=30
JWT_REFRESH_EXPIRATION_DAYS=30
JWT_RESET_PASSWORD_EXPIRATION_MINUTES=10
JWT_VERIFY_EMAIL_EXPIRATION_MINUTES=10

# Email Configuration (Production SMTP)
EMAIL_ENABLED=true
EMAIL_FROM=noreply@yourdomain.com

# SMTP Configuration
# Gmail için: smtp.gmail.com:587
# Outlook için: smtp-mail.outlook.com:587
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password

# Google Drive OAuth Configuration
# Google Cloud Console'dan OAuth 2.0 credentials oluşturun
# https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=https://yourdomain.com/v1/cloud-storage/google-drive/callback

# AWS Credentials Encryption Key (64 hex characters = 32 bytes)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AWS_CREDENTIALS_ENCRYPTION_KEY=your-64-character-hex-encryption-key

# Backup Storage Path
# Linux/macOS: /var/backups veya /opt/backups
# Windows: C:\backups
BACKUP_STORAGE_PATH=/var/backups

# Node Environment
NODE_ENV=production
