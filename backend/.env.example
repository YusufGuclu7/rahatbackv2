# Port number
PORT=3000

# App URL
APP_URL=http://localhost:3000

# Postgresql Prisma configuration options
DATABASE_URL=postgres://user:pass@127.0.0.1:5432/BoilerplateDB
# JWT
# JWT secret key
JWT_SECRET=thisisasamplesecret
# Number of minutes after which an access token expires
JWT_ACCESS_EXPIRATION_MINUTES=30
# Number of days after which a refresh token expires
JWT_REFRESH_EXPIRATION_DAYS=30
# Number of minutes after which a reset password token expires
JWT_RESET_PASSWORD_EXPIRATION_MINUTES=10
# Number of minutes after which a verify email token expires
JWT_VERIFY_EMAIL_EXPIRATION_MINUTES=10

# Mailgun configuration options for the email service
EMAIL_FROM=support@yourapp.com

MAILGUN_API_KEY=key-1234556789
MAILGUN_DOMAIN=mg.yourdomain.com
MAILGUN_HOST=https://api.eu.mailgun.net

# Database encryption key for storing credentials
DB_ENCRYPTION_KEY=your-32-character-secret-key-here-change-me

# ===================================================================================================
# AWS S3 CREDENTIALS ENCRYPTION KEY - CRITICAL SECURITY SETTING
# ===================================================================================================
# This key encrypts all AWS credentials (Access Key ID & Secret Access Key) stored in the database.
#
# IMPORTANT REQUIREMENTS:
# - Must be exactly 64 hexadecimal characters (a-f, 0-9)
# - MUST be set before first use - application will not start without it
# - MUST remain constant forever - changing it will make all stored credentials unreadable
# - Store this securely - if lost, all AWS credentials in database will be permanently lost
#
# TO GENERATE A SECURE KEY:
# Run this command in your terminal:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Example output (DO NOT USE THIS - generate your own!):
#   a1b2c3d4e5f6789abcdef1234567890abcdef1234567890abcdef1234567890
#
# PRODUCTION DEPLOYMENT:
# - Add this to your environment variables (not in git!)
# - Use secrets management (AWS Secrets Manager, HashiCorp Vault, etc.)
# - Never commit the actual key to version control
# - Keep a secure backup of this key in case of emergency
#
AWS_CREDENTIALS_ENCRYPTION_KEY=

# ===================================================================================================
# GOOGLE OAUTH CONFIGURATION - FOR GOOGLE DRIVE INTEGRATION
# ===================================================================================================
# Google Drive OAuth 2.0 credentials for backup storage integration
#
# SETUP INSTRUCTIONS:
# 1. Go to: https://console.cloud.google.com/apis/credentials
# 2. Create a new project or select existing one
# 3. Enable "Google Drive API" in APIs & Services
# 4. Create OAuth 2.0 Client ID:
#    - Application type: Web application
#    - Authorized redirect URIs: Add your callback URL (see below)
# 5. Copy Client ID and Client Secret here
#
# DEVELOPMENT:
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/v1/cloud-storage/google-drive/callback
#
# PRODUCTION:
# GOOGLE_REDIRECT_URI=https://yourdomain.com/v1/cloud-storage/google-drive/callback
#
# IMPORTANT FOR PRODUCTION:
# - Replace 'yourdomain.com' with your actual domain
# - Add the production URL to "Authorized redirect URIs" in Google Console
# - Use HTTPS (not HTTP) in production
# - Store credentials in environment variables, not in code
#
# AUTHORIZED REDIRECT URI EXAMPLES:
# - Development: http://localhost:3000/v1/cloud-storage/google-drive/callback
# - Staging: https://staging.yourdomain.com/v1/cloud-storage/google-drive/callback
# - Production: https://yourdomain.com/v1/cloud-storage/google-drive/callback
#
# Add ALL environments to "Authorized redirect URIs" in Google Console
# ===================================================================================================

# Backup storage path
BACKUP_STORAGE_PATH=./backups
